# Catalog Service Configuration
# Copy to .env and customize for your environment

# Service Configuration
PORT=5003
HOST=0.0.0.0
NODE_ENV=development

# Database
# Set to "true" to use in-memory database with snapshot seeding
CATALOG_USE_MEMORY_DB=true

# For persistent storage (leave unset for in-memory pg-mem)
# DATABASE_URL=postgresql://user:pass@localhost:5432/catalog

# Service URLs (resolved via @symbia/sys if not set)
# IDENTITY_SERVICE_URL=http://localhost:5001
# NETWORK_SERVICE_URL=http://localhost:5054
# LOGGING_SERVICE_URL=http://localhost:5002

# Rate limiting
# RATE_LIMIT_READ_MAX=1000
# RATE_LIMIT_WRITE_MAX=100
