name: nominal
description: >
  No faults. The baseline every other scenario is compared against, and the
  control for the regression suite: if assertions fire here, the checks
  themselves are wrong.
faults: []
expect:
  - id: pue_always_computable
    assert: platform_pue_is_computable_every_tick
    because: All meters healthy, so a refusal here is a false positive.
