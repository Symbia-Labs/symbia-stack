# Messaging Service Configuration
# Copy to .env and customize for your environment

# Service Configuration
PORT=5005
HOST=0.0.0.0
NODE_ENV=development

# Service URLs (resolved via @symbia/sys if not set)
# IDENTITY_SERVICE_URL=http://localhost:5001
# NETWORK_SERVICE_URL=http://localhost:5054
# ASSISTANTS_SERVICE_URL=http://localhost:5004

# Webhook for assistant notifications
# ASSISTANTS_WEBHOOK_URL=http://localhost:5004/api/webhook/messaging

# Database (leave unset for in-memory pg-mem)
# DATABASE_URL=postgresql://user:pass@localhost:5432/messaging

# WebSocket Configuration
# WS_PING_INTERVAL=25000
# WS_PING_TIMEOUT=5000

# Timeout Configuration
WEBHOOK_TIMEOUT_MS=10000
HTTP_REQUEST_TIMEOUT_MS=120000
SOCKET_PING_TIMEOUT_MS=60000
SOCKET_PING_INTERVAL_MS=25000
