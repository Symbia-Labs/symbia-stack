# Assistants Service Configuration
# Copy to .env and customize for your environment

# Service Configuration
PORT=5004
HOST=0.0.0.0
NODE_ENV=development

# Service URLs (resolved via @symbia/sys if not set)
# CATALOG_SERVICE_URL=http://localhost:5003
# IDENTITY_SERVICE_URL=http://localhost:5001
# NETWORK_SERVICE_URL=http://localhost:5054
# LOGGING_SERVICE_URL=http://localhost:5002

# Bootstrap assistants (enabled by default in development)
BOOTSTRAP_ASSISTANTS=true

# LLM Configuration
# OPENAI_API_KEY=your-openai-api-key
# ANTHROPIC_API_KEY=your-anthropic-api-key
# DEFAULT_LLM_PROVIDER=openai
# DEFAULT_LLM_MODEL=gpt-4

# Database (leave unset for in-memory pg-mem)
# DATABASE_URL=postgresql://user:pass@localhost:5432/assistants

# Agent Authentication (min 32 characters required, must match identity service)
# AGENT_CREDENTIAL=your-secure-credential-at-least-32-characters
